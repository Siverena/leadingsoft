// import { useContext } from '@nuxtjs/composition-api';

// const { app } = useContext();
// const baseURL = app.context.baseURL;
// const getStaticImageUrl = (src) => {
//     return `${baseURL}images/${src}`;
// };
// const getImageUrl = (src) => {
//     return `${baseURL}assets/img/${src}`;
// };

const getImageUrl = () => {
    console.log('хаюшки');
};
export default {
    getImageUrl,
};
